@model UserSearchViewModel

<partial name="_BreadCrumbs" model=@(new [] {
    ("/","Home"),
    ("","Users")
}) />

<partial name = "_ViewLayouts">
<partial name = "_SearchUser">

<h3 class="navbar-title mb-3">User List</h3>

<div>
<a asp-condition=@(User.IsInRole("admin")) class="btn btn-sm btn-danger my-3" asp-action="Create" >
    <i class="bi bi-plus"></i> Create
</a>  
</div>   

@* <table class="table table-hover">
    <thead>
        <tr>
            <th>Id</th>
            <th>Name</th>
            <th>Email</th>
            <th>Role</th>
            <th>Nationality</th>
        </tr>
    </thead>
    <tbody> *@
        @* @foreach(var u in Model) {
            <tr>
                <td>@u.Id</td>
                <td>@u.Name</td>
                <td>@u.Email</td>
                <td>@u.Role</td>
                <td>@u.Nationality</td>
                
                <td> *@


    <div class="card-deck">
         @foreach (var r in Model.Users) 
         {
            <div class="row row-cols-1 row-cols-md-3">
               <div class= "card text-center mb-3 bg-light shadow-lg" style="width:18rem;">
                <img class="recipeimage card-img-top img-responsive" src="@r.PhotoUrl" alt= "">
                  <div class="card-body">
                  <h6 class="card-title navbar-title">
                     @r.Name
                  </h6>

                  <p class = "card-text">
                     @r.Email
                     @r.Role
                     @r.Nationality
                  </p>             
            </div>

            <div class="card-footer shadow-lg text-muted">
               <a asp-controller="Recipe" asp-action="Edit" asp-route-id=@r.Id><i class="bi bi-pen"></i>Edit</a>
               <a asp-action="Details" asp-route-id=@r.Id><i class="bi bi-info-circle"></i> Details</a>
               <a asp-controller="User" asp-action="RecipeDelete" asp-route-id=@r.Id><i class="bi bi-trash"></i> Delete</a>
            </div>
      </div>
   </div>
}
</div>
                    @* <!-- Add View link -->
                    <a data-bs-toggle="tooltip" data-bs-placement="top" title="view User details" asp-controller="User" asp-action="Details" asp-route-id=@u.Id><i class="bi bi-display"></i></a>  
                    <!-- Add edit link  -->
                    <a asp-condition=@(User.IsInRole("admin")) asp-action="Edit" asp-route-id=@u.Id><i class="bi bi-pen"></i></a>
                    <!-- Add delete link -->
                    <a asp-condition=@User.IsInRole("admin") asp-action="Delete" asp-route-id=@u.Id><i class="bi bi-trash"></i></a>
                </td>
            </tr>
        }        
    </tbody>
</table> *@